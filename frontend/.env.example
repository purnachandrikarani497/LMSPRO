# Public API base (Vite dev usually proxies /api — leave default)
# VITE_API_BASE_URL=/api

# Google Sign-In — Web client ID from Google Cloud Console (same value as backend GOOGLE_CLIENT_ID)
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
